{
  "rules": {
    "기록": {
      ".read": true,
      ".write": true,
      "$result": {
        ".validate": "newData.hasChildren(['nickname', 'score', 'resultType', 'timestamp', 'date'])"
      }
    }
  }
} 